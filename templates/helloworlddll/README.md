# Hello World Dll
This is a hello world base template for a dynamically linked library

## Compile
Build with cmake
```bash
cmake -S src -B build
cmake --build build
```

Build with cmakeauto
```bash
cmakeauto build
```
